# 웹 개발 완벽 가이드: HTML부터 React까지

## 목차
1. [학습 로드맵](#1-학습-로드맵)
2. [HTML/CSS 기초](#2-htmlcss-기초)
3. [JavaScript 기초](#3-javascript-기초)
4. [React 기초](#4-react-기초)
5. [실전 프로젝트 분석](#5-실전-프로젝트-분석)

## 1. 학습 로드맵

### Phase 1: 웹 기초 (2-3주)
1. HTML 기초
   - 기본 태그 학습
   - 시맨틱 마크업
   - 폼과 입력 요소

2. CSS 기초
   - 선택자와 속성
   - 박스 모델
   - Flexbox 레이아웃
   - 반응형 디자인

### Phase 2: JavaScript (3-4주)
1. 기본 문법
   - 변수와 데이터 타입
   - 함수와 스코프
   - 조건문과 반복문

2. 심화 개념
   - 배열 메서드
   - 객체와 클래스
   - 비동기 프로그래밍
   - ES6+ 문법

### Phase 3: React 입문 (4주)
1. React 기초
   - JSX 문법
   - 컴포넌트 개념
   - Props와 State

2. React 심화
   - Hooks 사용법
   - 컴포넌트 생명주기
   - 상태 관리
   - 라우팅

### Phase 4: 실전 프로젝트 (2-3주)
1. 프로젝트 설정
   - 개발 환경 구축
   - 패키지 관리
   - 프로젝트 구조

2. 기능 구현
   - 컴포넌트 설계
   - API 연동
   - 상태 관리
   - 스타일링

## 2. HTML/CSS 기초

### HTML 기본 구조
```html
<!DOCTYPE html>
<html>
<head>
    <title>페이지 제목</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <header>
        <h1>웹사이트 제목</h1>
        <nav>
            <ul>
                <li><a href="#home">홈</a></li>
                <li><a href="#about">소개</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <section>
            <h2>섹션 제목</h2>
            <p>내용이 들어갑니다.</p>
        </section>
    </main>
    <footer>
        <p>푸터 내용</p>
    </footer>
</body>
</html>

### CSS 기초 문법
```css
/* 기본 선택자 */
.class-name {    /* 클래스 선택자 */
    property: value;
}

#id-name {      /* ID 선택자 */
    property: value;
}

/* Flexbox 레이아웃 */
.container {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
}

/* 반응형 디자인 */
@media (max-width: 768px) {
    .container {
        flex-direction: column;
    }
}
```

## 3. JavaScript 기초

### 기본 문법
```javascript
// 변수 선언
let name = "홍길동";
const age = 25;

// 함수 정의
function greet(name) {
    return `안녕하세요, ${name}님!`;
}

// 화살표 함수
const multiply = (a, b) => a * b;

// 배열 메서드
const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map(num => num * 2);
const evenNumbers = numbers.filter(num => num % 2 === 0);

// 비동기 처리
async function fetchData() {
    try {
        const response = await fetch('https://api.example.com/data');
        const data = await response.json();
        return data;
    } catch (error) {
        console.error('에러:', error);
    }
}
```

## 4. React 기초

### 컴포넌트 기본 구조
```jsx
import React, { useState, useEffect } from 'react';

const MyComponent = ({ title }) => {
    // 상태 관리
    const [count, setCount] = useState(0);
    
    // 생명주기 관리
    useEffect(() => {
        console.log('컴포넌트가 마운트됨');
        return () => {
            console.log('컴포넌트가 언마운트됨');
        };
    }, []);
    
    // 이벤트 핸들러
    const handleClick = () => {
        setCount(prev => prev + 1);
    };
    
    // JSX 반환
    return (
        <div>
            <h1>{title}</h1>
            <p>카운트: {count}</p>
            <button onClick={handleClick}>
                증가
            </button>
        </div>
    );
};

export default MyComponent;
```

## 5. 실전 프로젝트 분석

### 프로젝트 구조
```plaintext
myweb/
├── src/
│   ├── components/
│   │   └── TempPage.jsx
│   ├── styles/
│   │   └── TempPageStyles.css
│   └── App.js
└── package.json
```

### package.json 분석
```json
{
  "dependencies": {
    "react": "^19.1.0",        // React 코어
    "react-dom": "^19.1.0",    // 웹 렌더링
    "react-router-dom": "^7.4.1", // 라우팅
    "axios": "^1.8.4"          // HTTP 클라이언트
  }
}
```

### 주요 컴포넌트 분석
```jsx
// TempPage.jsx
const TempPage = () => {
    const [logoutError, setLogoutError] = useState('');
    const navigate = useNavigate();

    const handleLogout = async () => {
        try {
            await axios.post('/auth/logout');
            localStorage.removeItem('user');
            navigate('/');
        } catch (error) {
            setLogoutError('로그아웃 실패');
        }
    };

    return (
        <div className="community-page">
            <header>
                <nav>{/* 네비게이션 내용 */}</nav>
            </header>
            <main>
                <section>{/* 메인 콘텐츠 */}</section>
            </main>
        </div>
    );
};
```

### 스타일링 예시
```css
/* TempPageStyles.css */
.community-page {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

.header {
    display: flex;
    justify-content: space-between;
    padding: 1rem;
    background-color: #4AD395;
}

/* 반응형 디자인 */
@media (max-width: 768px) {
    .main-content {
        padding: 1rem;
    }
}
```

## 학습 시 주의사항

1. 단계별 학습
   - 기초부터 차근차근 진행
   - 각 단계별 충분한 실습
   - 개념 이해 후 다음 단계로 진행

2. 실습 중심 학습
   - 코드 직접 작성
   - 예제 변형해보기
   - 작은 프로젝트 만들기

3. 문제 해결 능력
   - 오류 메시지 이해하기
   - 디버깅 도구 활용
   - 문서 참조 습관

4. 개발 도구 활용
   - VS Code 단축키 학습
   - 개발자 도구 활용
   - Git 버전 관리

## 추천 학습 자료

1. 공식 문서
   - [MDN Web Docs](https://developer.mozilla.org/)
   - [React 공식 문서](https://reactjs.org/)

2. 온라인 강의 플랫폼
   - Udemy
   - Coursera
   - freeCodeCamp

3. 커뮤니티
   - Stack Overflow
   - GitHub
   - Dev.to
```

이 가이드를 통해 웹 개발을 체계적으로 학습하실 수 있습니다. 각 단계별로 필요한 개념과 실습 내용을 포함하고 있으며, 실제 프로젝트 코드를 분석하여 실무적인 이해를 돕습니다.
